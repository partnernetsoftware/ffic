define(
		fib(n) 
		if(cmp(n 2) 
			1
			if(cmp(n 1) 
				1
				add(fib(sub(n 1)) fib(sub(n 2)))
				)
			)
		);

;define(testadd(n) add(n 1));
;testadd(3);
;
;define(testsub(n) sub(n 1));
;testsub(4);

fib(4);

;print( "2222")
;print( fib( 10))
;	;print(fib(30))

exit()
